import type { ModuleTree, MutationTree, ActionTree, GetterTree, ModuleOptions, StoreOptions } from 'vuex';
import type { State } from './types';

const state: State = {
    a: 1
};

export default state;
